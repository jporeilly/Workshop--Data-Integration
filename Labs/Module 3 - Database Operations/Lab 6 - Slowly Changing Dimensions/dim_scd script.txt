Required scripts:

CREATE TABLE PUBLIC.DIM_SCD (
	TK BIGINT NOT NULL,
	VERSION INTEGER,
	DATE_FROM TIMESTAMP,
	DATE_TO TIMESTAMP,
	ID INTEGER,
	CITY VARCHAR(20),
	LAST_UPDATE TIMESTAMP,
	PRIMARY KEY (TK)
);


